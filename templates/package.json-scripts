{
  "scripts": {
    "test:e2e": "cypress run",
    "test:e2e:dev": "cypress open",
    "test:e2e:prod": "cypress run --config baseUrl=https://yourdomain.com",
    "test:e2e:local": "npm run build && npm run preview & sleep 5 && cypress run --config baseUrl=http://localhost:4173 && kill %1"
  }
}
